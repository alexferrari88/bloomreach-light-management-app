// This is a simple placeholder hook
// Replace with a proper theme implementation when you add dark mode
export function useTheme() {
  return {
    theme: "light",
    setTheme: (_theme: string) => {},
    resolvedTheme: "light",
  };
}
